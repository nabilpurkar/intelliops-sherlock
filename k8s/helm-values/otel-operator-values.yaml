manager:
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"

admissionWebhooks:
  certManager:
    enabled: true
  autoGenerateCert:
    enabled: false

serviceMonitor:
  enabled: false
